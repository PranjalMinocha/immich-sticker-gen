# serving/system/Dockerfile.pipeline
FROM python:3.11-slim

RUN pip install --no-cache-dir \
    "ray[client,train]==2.10.0" \
    boto3 \
    numpy \
    Pillow \
    requests

COPY pipeline.py /app/pipeline.py
WORKDIR /app

CMD ["python", "pipeline.py"]
